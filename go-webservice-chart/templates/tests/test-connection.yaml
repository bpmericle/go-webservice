apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-webservice-chart.fullname" . }}-test-connection"
  labels:
{{ include "go-webservice-chart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "go-webservice-chart.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
